# FrameGuide deploy branch

This isolated branch contains the complete FrameGuide source archive and a build bootstrap. During `npm run build`, `scripts/unpack.mjs` extracts the full Next.js project and runs `next build`.

Production source archive: `frameguide-full.tar.gz`.
